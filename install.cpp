compile:
  text()
  as(f)
  inytrofrt(x - 6 ad hj on int plat)
    on;
  std
  background(0x.BLUE)
    .text
    .buttons.list
  onclick install EXCEPT firstStart;
  EXCEPT::
  compi
  ext on ext wipe background;
